#include <iostream>

using namespace std;

int main(int argc, char* argv[])
{
	setlocale(LC_ALL, "Russian");
	int n = 0;
	cin >> n;
	if (n < 1000)
	{
		cout << "n == ? banana";
	}
	else
	{
		cout << "no banana";
	}
	switch (n)
	return EXIT_SUCCESS;
}